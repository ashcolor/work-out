{
  "name": "work-out",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler pages dev",
    "build": "cd frontend && npm run build",
    "deploy": "npm run build && wrangler pages deploy",
    "db:init": "wrangler d1 execute work-out --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute work-out --remote --file=schema.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@hono/node-server": "^1.19.12",
    "hono": "^4.12.10",
    "wrangler": "^4.80.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260404.1"
  }
}
